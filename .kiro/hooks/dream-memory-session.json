{
  "version": "v1",
  "hooks": [
    {
      "name": "Dream memory readiness",
      "description": "Report whether a memory consolidation is due. Starts no LLM job.",
      "trigger": "SessionStart",
      "action": {
        "type": "command",
        "command": "test -z \"$(find .kiro/dreaming/.last-dream -mtime -1 2>/dev/null)\" && echo 'Memory: a dream is due (no consolidation in the last 24h). Offer /dream at a natural stopping point.' || true"
      },
      "timeout": 10
    }
  ]
}
