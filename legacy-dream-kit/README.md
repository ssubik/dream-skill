Superseded machinery from the versioned-store design (Python helper, immutable
stores, promotion/rollback, unit tests). Kept only so nothing is lost, since this
project is not under version control. Delete this folder when you no longer want it.
