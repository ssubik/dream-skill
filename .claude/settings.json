{
  "hooks": {
    "SessionEnd": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "touch .claude/dreaming/.dream-pending"
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "matcher": "startup|resume|clear",
        "hooks": [
          {
            "type": "command",
            "command": "if [ -e .claude/dreaming/.dream-pending ] || [ ! -e .claude/dreaming/.last-dream ]; then echo 'Memory: the previous session ended without consolidating. Run the dream skill now, before the user request, then continue.'; fi"
          }
        ]
      }
    ]
  },
  "permissions": {
    "allow": [
      "Bash(cat .claude/dreaming/MEMORY.md)",
      "Bash(ls .claude/dreaming/topics)"
    ]
  }
}
